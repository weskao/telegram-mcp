repos:
  - repo: https://github.com/psf/black
    rev: 25.9.0
    hooks:
      - id: black
        language_version: python3

  - repo: https://github.com/pycqa/flake8
    rev: 7.3.0
    hooks:
      - id: flake8
        args: ["--count", "--select=E9,F63,F7,F82", "--show-source", "--statistics"]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-merge-conflict

  - repo: https://github.com/hadolint/hadolint
    rev: v2.12.0
    hooks:
      - id: hadolint-docker
        args: ["--failure-threshold", "error", "Dockerfile"]

  - repo: local
    hooks:
      - id: docker-compose-config
        name: docker compose config
        entry: docker compose --env-file .env.example config
        language: system
        pass_filenames: false
        stages: [pre-commit]
      - id: docker-build-ci
        name: docker build (ci parity)
        entry: docker build --file Dockerfile --tag telegram-mcp:ci .
        language: system
        pass_filenames: false
        stages: [pre-push]
      - id: pytest-coverage-ci
        name: pytest with coverage (ci parity)
        entry: uv run --env-file .env.example pytest --cov --cov-report=term-missing --cov-report=xml
        language: system
        pass_filenames: false
        stages: [pre-push]
      - id: sync-upstream-readme
        name: refresh README.upstream.md after merge
        entry: >-
          sh -c 'git remote get-url upstream >/dev/null 2>&1 &&
          [ -f README.upstream.md ] && make sync-upstream-readme || true'
        language: system
        pass_filenames: false
        always_run: true
        stages: [post-merge]
