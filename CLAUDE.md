# Project instructions

## Version numbering

The upstream author already tags releases as `v<version_number>`. To keep this
project's version numbers distinguishable from the upstream author's tags,
this project's version numbers are written as plain numbers only, without a
leading `v` — e.g. `1.2.34`.
